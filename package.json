{
  "name": "context-bridge-monorepo",
  "description": "Industrial-grade Context Bridge MCP Server for OpenClaw - The 'Hive Mind' for Agent Swarms.",
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
